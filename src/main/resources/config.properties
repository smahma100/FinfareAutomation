# Application URL
base.url=https://www.google.com/finance

# Test data for stock symbols
test.data=NFLX,MSFT,TSLA,EKGYO,TUPRS

# Report settings (optional, if needed for flexibility)
report.path=reports/extent-report.html

# WebDriver settings (optional, can add if using specific configurations)
browser=chrome
timeout.seconds=10
